name: Legacy Bulk Question Patch (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy patch is intentionally disabled
        run: echo "ASCENT core is protected. This legacy self-modifying workflow no longer edits production files."
