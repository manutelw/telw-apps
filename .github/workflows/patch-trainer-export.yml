name: Patch live trainer leaderboard

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch trainer leaderboard
        run: python .github/scripts/patch_trainer_leaderboard.py
      - name: Fix incomplete-response due count
        run: python .github/scripts/fix_incomplete_response_due.py
      - name: Fix Custom dashboard metrics
        run: python .github/scripts/patch_dashboard_custom_metrics.py
      - name: Remove extra Excel compliance tabs
        run: python .github/scripts/remove_excel_compliance_tabs.py
      - name: Clarify dashboard completion labels
        run: python .github/scripts/rename_dashboard_completion_labels.py
      - name: Add dashboard median mode and insight
        run: python .github/scripts/patch_dashboard_batch_stats.py
      - name: Commit trainer fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ascent/trainer.html
          if git diff --cached --quiet; then
            echo "No trainer changes required"
            exit 0
          fi
          git commit -m "Fix live dashboard and Excel leaderboard"
          git pull --rebase origin main
          git push origin HEAD:main
