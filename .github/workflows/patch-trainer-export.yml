name: Legacy Trainer Export Patch (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy patch is intentionally disabled
        run: echo "ASCENT trainer/export core is protected. Export changes must now be made explicitly in reviewed source."
