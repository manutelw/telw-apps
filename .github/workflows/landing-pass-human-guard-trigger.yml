name: Landing Pass Guard Trigger
on:
  pull_request:
    paths: ['index.html']
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Landing-only change; ASCENT Integrity Guard is the governing check."
