name: Legacy Trainer Cache Bust (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy patch is intentionally disabled
        run: echo "ASCENT trainer core is protected. Cache-bust changes must now be made explicitly in reviewed source."
