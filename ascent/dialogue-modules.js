window.ASCENT_DIALOGUE_MODULES_READY=(async()=>{const names=['dialogue-modules-data-1.txt','dialogue-modules-data-2.txt','dialogue-modules-data-3.txt'];const parts=await Promise.all(names.map(n=>fetch('./'+n,{cache:'force-cache'}).then(r=>{if(!r.ok)throw new Error('Module data could not load');return r.text()})));const b=parts.join('');const a=Uint8Array.from(atob(b),c=>c.charCodeAt(0));const ds=new DecompressionStream('gzip');const txt=await new Response(new Blob([a]).stream().pipeThrough(ds)).text();window.ASCENT_DIALOGUE_MODULES=JSON.parse(txt);return window.ASCENT_DIALOGUE_MODULES;})();
